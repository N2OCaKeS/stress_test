version: '3.8'

services:
  devpi2:
    build:
      context: .
    ports:
      - "3141:3141"
    volumes:
      - devpi-data2:/data
    env_file:
      - .env      
    restart: always
    
volumes:
  devpi-data2:
