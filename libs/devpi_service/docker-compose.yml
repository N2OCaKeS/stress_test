version: '3'

services:
  devpi:
    build:
      context: .
    ports:
      - "3141:3141"
    volumes:
      - devpi-data:/data
      - /home/u/tokens.json:/git/tokens.json
    env_file:
      - .env      
    restart: always
    
volumes:
  devpi-data:
