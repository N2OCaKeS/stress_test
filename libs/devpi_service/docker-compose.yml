version: '3.8'

services:
  devpi:
    build:
      context: .
    ports:
      - "3141:3141"
    volumes:
      - devpi-data:/data
    env_file:
      - .env      
    restart: always
    
volumes:
  devpi-data:
