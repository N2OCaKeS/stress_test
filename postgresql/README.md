# PostgreSQL_benchmark
Комплекс нагрузочного тестирования, направленный на тестирование БД PostgreSQL.


### Test 1
### PostgreSQL_benchmark
#### Кодовое обозначение: `postgresql`   
Система нагрузочного тестирования БД PostgreSQL с использованием функционала **pgbench**.   
Подключение к базе происходит при помощи сетевого сокета.  
Выполняется в режиме **орел**.  
Используется один итерационный цикл по кол-ву клиентов с зафиксированными параметрами.   
  - масштабирование = 500 
  - транзакции = 100000 
  - потоки = 200 
  - клиенты = 100 - 500 (шаг равен 100)


### Test 2
### PostgreSQL_benchmark audit-off
#### Кодовое обозначение: `postgresql-aud-off`   
Система нагрузочного тестирования БД PostgreSQL с использованием функционала **pgbench**.   
Подключение к базе происходит при помощи сетевого сокета.  
Выполняется в режиме **орел с отключенным аудитом `audit=0`**.    
Используется один итерационный цикл по кол-ву клиентов с зафиксированными параметрами.   
  - масштабирование = 500 
  - транзакции = 100000 
  - потоки = 200 
  - клиенты = 100 - 500 (шаг равен 100)


### Test 3
### PostgreSQL_benchmark balance
#### Кодовое обозначение: `psql balance`   
Тестирование работы PostgreSQL в отказоустойчивом кластере и обновление БД при сохранении доступности.  
Используется самописный аналог **pgbench** т.к. стандартный при отключении БД падает с ошибкой.  
Достижение кворума гарантируется путём размещения трёх экземпляров базы данных и трёх балансировщиков нагрузки,  
введенных в область домена.  
Результируещем показателем является количество проавленных запросов относительно их общего числа.  


### Test 4
### PostgreSQL_benchmark kernels
#### Кодовое обозначение: `psql kernels`   
Система нагрузочного тестирования БД PostgreSQL с использованием функционала **pgbench**.  
При тестирвоании производится проверка деградации производительности БД на многоядерных системах.  
Тестирование проводится на 8, 16, 24 и 32 ядрах. В последствии производится оценка роста производительности  
при увеличении количества ядер (критерий прохождения на данный момент: 15% к предыдущему).   
Расчет усредненных показателей производится по данной методике:

-  [Расчет результатов PGbench](https://life.astralinux.ru/pages/viewpage.action?pageId=237097452&src=contextnavpagetreemode)


### Test 5
### PostgreSQL_benchmark parsec
#### Кодовое обозначение: `psql parsec`   
Система нагрузочного тестирования БД PostgreSQL с использованием **доработанного под parsec** функционала **pgbench**.   
Подключение к базе происходит при помощи сетевого сокета.  
Выполняется в режиме **смоленск**.  
Используется один итерационный цикл по кол-ву клиентов с зафиксированными параметрами.   
  - масштабирование = 500 
  - транзакции = 100000 
  - потоки = 200 
  - клиенты = 100 - 500 (шаг равен 100)


### Test 6
### PostgreSQL_benchmark smol
#### Кодовое обозначение: `postgresql-sm`   
Система нагрузочного тестирования БД PostgreSQL с использованием функционала **pgbench**.   
Подключение к базе происходит при помощи сетевого сокета.  
Выполняется в режиме **смоленск**.  
Используется один итерационный цикл по кол-ву клиентов с зафиксированными параметрами.   
  - масштабирование = 500 
  - транзакции = 100000 
  - потоки = 200 
  - клиенты = 100 - 500 (шаг равен 100)


### Test 7
### PostgreSQL_benchmark vanilla
#### Кодовое обозначение: `psql vanilla`   
Система нагрузочного тестирования **ванильной** БД PostgreSQL с использованием функционала **pgbench**.   
Подключение к базе происходит при помощи сетевого сокета.  
Выполняется в режиме **орел**.  
Используется один итерационный цикл по кол-ву клиентов с зафиксированными параметрами.   
  - масштабирование = 500 
  - транзакции = 100000 
  - потоки = 200 
  - клиенты = 100 - 500 (шаг равен 100)


### Test 8
### Tantor_benchmark kernels
#### Кодовое обозначение: `tantor kernels`   
Система нагрузочного тестирования БД Tantor с использованием функционала **pgbench**.  
При тестирвоании производится проверка деградации производительности БД на многоядерных системах.  
Тестирование проводится на 8, 16, 24 и 32 ядрах. В последствии производится оценка роста производительности  
при увеличении количества ядер (критерий прохождения на данный момент: 15% к предыдущему).   
Расчет усредненных показателей производится по данной методике:

-  [Расчет результатов PGbench](https://life.astralinux.ru/pages/viewpage.action?pageId=237097452&src=contextnavpagetreemode)


### Test 9
### Tantor_benchmark vanilla
#### Кодовое обозначение: `tantor vanilla`   
Система нагрузочного тестирования **ванильной** БД Tantor с использованием функционала **pgbench**.   
Подключение к базе происходит при помощи сетевого сокета.  
Выполняется в режиме **орел**.  
Используется один итерационный цикл по кол-ву клиентов с зафиксированными параметрами.   
  - масштабирование = 500 
  - транзакции = 100000 
  - потоки = 200 
  - клиенты = 100 - 500 (шаг равен 100)


### Test 10
### PostgreSQL_benchmark OOM
#### Кодовое обозначение: `psql oom`
Тестирование работы PostgreSQL на наличие краха СУБД. Нагрузка осуществляется путем обработки большого количества    
входных строк (~20000) во временных таблицах.    
Результат считается успешным, если во время работы SQL скрипта не произошел крах СУБД или ООМ.    


### Test 11
### PostgreSQL_OLAP
#### Кодовое обозначение: `psql olap`
Тестирование работы PostgreSQL c olap запросами, и замером времени каждого запроса. Нагрузка идет ступенчато сначала 1 запрос далее 10, потом 100 и 1000 и 10000. В тестировании участвуют сложные аналитические запросы.


#### Общие зависимости

- atlassian-python-api 
- asyncio
- pretty-html-table 
- pandas 
- pysnooper
- psycopg
- psycopg_pool
- scikit-learn 
- requests
- numpy
- matplotlib
- lxml
- bs4
- prettytable

#### Общие базовые настройки сервера
В тестах используется дефолтный файл postgresql.conf с следующими изменениями:    

-  max_connections = 200
-  shared_buffers = 64512MB
-  effective_cache_size = 193536MB
-  maintenance_work_mem = 2GB
-  checkpoint_completion_target = 0.9
-  wal_buffers = 16MB
-  default_statistics_target = 100
-  random_page_cost = 1.1
-  effective_io_concurrency = 200/g'
-  work_mem = 82574kB
-  min_wal_size = 1GB
-  max_wal_size = 4GB
-  max_worker_processes = 32
-  max_parallel_workers_per_gather = 4
-  max_parallel_workers = 32
-  max_parallel_maintenance_workers = 4

#### Проведение тестирования
Тестирование проводится в автоматическом режиме с помощью оркестратора ALLTA.

- [ALLTA](https://life.astralinux.ru/pages/viewpage.action?pageId=253888776&src=contextnavpagetreemode)

#### Обработка результатов
Результаты обрабатываются и выкладываются в пространстве нагрузочного тестирования confluence 
с помощью орекстратора Bendiks в автоматическом режиме.

- [Пространство](https://life.astralinux.ru/pages/viewpage.action?pageId=140673304&src=contextnavpagetreemode)
- [Статистика](https://life.astralinux.ru/pages/viewpage.action?pageId=140674766&src=contextnavpagetreemode)