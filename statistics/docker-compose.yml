version: "3.7"
services:
  app:
    build:
      context: .
    container_name: fastapi_app_statistics
    command: ["gunicorn", "main_api:app", "--workers", "4", "--worker-class", "uvicorn.workers.UvicornWorker", "--bind=0.0.0.0:8000"]
    ports:
      - 7777:8000