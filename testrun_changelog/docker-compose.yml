version: "3.7"
services:
  app:
    build:
      context: .
    container_name: testrun_create_changelog
    command: ["gunicorn", "app:app", "--workers", "4", "--worker-class", "uvicorn.workers.UvicornWorker", "--bind=0.0.0.0:8000"]
    ports:
      - 8989:8000